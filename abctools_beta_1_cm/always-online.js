// Assume always online (to avoid having to load piano and percussion in all cases)
function doOnlineCheck(){
	gSamplesOnline = true;
	//console.log("always-online.js - gSamplesOnLine = "+gSamplesOnline);
}

